<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Login Your Account</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
      /* Background image with overlay */
      body {
        background: url("https://media.istockphoto.com/id/1286943539/photo/location-icons-of-gps-navigation-global-5g-high-speed-internet-connection-and-big-data.jpg?s=612x612&w=0&k=20&c=S7o0aRX-GEr1ytqmvK4HiMGObZNwyUv8spDC84hcHJ8=")
          no-repeat center center/cover;
      }
    </style>
  </head>
  <body class="flex items-center justify-center min-h-screen relative">
    <!-- Overlay -->
    <div
      class="absolute inset-0 bg-gradient-to-br from-indigo-900/60 via-purple-900/40 to-pink-900/60"
    ></div>

    <!-- Login Card -->
    <div
      class="relative w-full max-w-md bg-white/20 backdrop-blur-xl p-8 rounded-3xl shadow-2xl border border-white/30"
    >
      <!-- Heading -->
      <h2
        class="text-3xl font-extrabold text-center mb-6 bg-gradient-to-r from-indigo-400 via-purple-400 to-pink-400 text-transparent bg-clip-text drop-shadow"
      >
        Login Your Account
      </h2>

      <!-- Form -->
      <form action="/login" method="post" class="space-y-5">
        <!-- Email -->
        <div>
          <input
            type="email"
            placeholder="Email"
            name="email"
            class="w-full px-4 py-3 border border-white/40 bg-white/30 rounded-xl text-gray-800 placeholder-gray-600 focus:outline-none focus:ring-2 focus:ring-purple-400 shadow-sm"
            required
          />
        </div>

        <!-- Password -->
        <div>
          <input
            type="password"
            placeholder="Password"
            name="password"
            class="w-full px-4 py-3 border border-white/40 bg-white/30 rounded-xl text-gray-800 placeholder-gray-600 focus:outline-none focus:ring-2 focus:ring-purple-400 shadow-sm"
            required
          />
        </div>

        <!-- Submit Button -->
        <button
          type="submit"
          class="w-full py-3 rounded-xl bg-gradient-to-r from-indigo-500 via-purple-500 to-pink-500 text-white font-semibold shadow-lg hover:opacity-90 transition"
        >
          🚀 Login Account
        </button>

        <!-- Divider -->
        <div class="flex items-center my-4">
          <hr class="flex-1 border-white/40" />
          <span class="px-3 text-white/70 text-sm">or</span>
          <hr class="flex-1 border-white/40" />
        </div>

        <!-- Register Link -->
        <p class="text-center text-white/90 text-sm">
          Don’t have an account?
          <a href="/" class="text-pink-300 font-medium hover:underline"
            >Register</a
          >
        </p>
      </form>
    </div>
  </body>
</html>
